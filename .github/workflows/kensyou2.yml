name: kensyou2
on: push

jobs:
  hensuu-job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "test-jobが実行される"
      - run: echo "${{ env.hensuu }}"
      - run: echo "$hensuu"
  working-directory-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: pwd
      - run: ls
  name-job:
    runs-on: ubuntu-latest
    steps:
      - run: pwd
  # secret-mask-clear-job:
  #   runs-on: job
  #   steps:
  #    - run: |
  #       echo "${{ secrets.MY_SECRET }}" > temp.txt
  #       cat temp.txt
  multi-step-job:
    runs-on: ubuntu-latest
    steps:
      - run:
          echo ${{ github.sha }}
          echo ${{ github.sha }}

